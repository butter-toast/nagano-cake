<div class='container'>
  <h5 class="col-3 my-5 my-3 bg-light fw-bold font-weight-bold">ログイン</h5>
  <h6 class="col-3 ml-5 my-3 bg-light text-center">会員の方はこちらからログイン</h6>
  <div class="col-sm-5 mx-auto">
  <%= form_with model: @customer, url: customer_session_path do |f| %>
    <div class="field">
      <%= f.label :メールアドレス %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
    </div>
    <div class="field mt-2">
      <%= f.label :パスワード %>
      <%= f.password_field :password, class: "mb-4", autocomplete: "current-password" %>
    </div>
    <div class="actions">
      <%= f.submit "ログイン", class: "btn btn-primary" %>
    </div>
  <% end %>
  </div>
  <h6 class="col-3 ml-5 my-3 bg-light text-center">会員登録がお済みでない方</h6>
  <div class="col-sm-5 mx-auto mb-5">
    <%= render "public/shared/links" %>
  </div>
</div>
